<Window x:Class="ElectricalPowerSystems.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectricalPowerSystems"
        mc:Ignorable="d"
        Title="ElectricalPowerSystems v 1.0 - 0.(9)" Height="360" Width="531.916" MinWidth="400" MinHeight="400">
    <Grid>
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Height="25" VerticalAlignment="Top">
            <MenuItem Header="File">
                <MenuItem Header="New Project" ></MenuItem>
                <MenuItem Header="Open Project" />
                <MenuItem Header="Open Recent" >
                    <MenuItem Header="Clear list" IsEnabled="False">
                    </MenuItem>
                    <Separator />
                </MenuItem>
            <Separator />
                <MenuItem Header="Exit" ></MenuItem>
            </MenuItem>
            <MenuItem Header="Edit" >
                <MenuItem Header="Undo" Command="Undo" IsEnabled="False"></MenuItem>
                <MenuItem Header="Redo" Command="Redo" IsEnabled="False"></MenuItem>
                <MenuItem Header="Copy" Command="Copy" ></MenuItem>
                <MenuItem Header="Cut" Command="Cut" ></MenuItem>
                <MenuItem Header="Paste" Command="Paste" ></MenuItem>
                
                
                
            </MenuItem>
            <MenuItem x:Name="RunMenuButton" Header="Run" Click="RunMenuButton_Click" ></MenuItem>
            <MenuItem x:Name="CheckSyntaxMenuButton" Header="Check syntax" ></MenuItem>
            <MenuItem Header="Help" ></MenuItem>
        </Menu>
        <TextBox Grid.Row="1" TextWrapping="Wrap" Height="Auto" Width="Auto" Margin="10" FontFamily="Courier New" Text="Possible format:&#xD;&#xA;element=resistor(value,nodeLabel1,nodeLabel2)&#xD;&#xA;element=voltageSource(value,nodeLabel1,nodeLabel2)&#xD;&#xA;element=ground(nodeLabel)&#xD;&#xA;element=capacitor(value,nodeLabel1,nodeLabel2)&#xD;&#xA;element=inductor(value,nodeLabel1,nodeLabel2)&#xD;&#xA;element=currentSource(value,nodeLabel1,nodeLabel2)&#xD;&#xA;element=node(nodeLabel)&#xD;&#xA;element=line(nodeLabel1,nodeLabel2) - zero resistance&#xD;&#xA;output:&#xD;&#xA;current(element)&#xD;&#xA;voltage(element) - voltage drop on element&#xD;&#xA;voltage(node) - relative voltage on node&#xD;&#xA;">
            <TextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Копировать"></MenuItem>
                    <MenuItem Header="Вставить"></MenuItem>
                    <MenuItem Header="Вырезать"></MenuItem>
                    <MenuItem Header="Удалить"></MenuItem>
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
        <TabControl Grid.Row="2" >
            <TabItem Header="Output">
                <RichTextBox Grid.Row="3" Height="Auto" Width="Auto" Margin="10">
                    <FlowDocument>
                        <Paragraph>
                            <Run Text=""/>
                        </Paragraph>
                    </FlowDocument>
                </RichTextBox>
            </TabItem>
            <TabItem Header="Errors">
                <ListBox Margin="10"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
